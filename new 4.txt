{fields.map((f,index) =>{
                 return <isDocumentRequiredAppVerType index={index} control=[control]/>
          }
            
              


 {visibel ? fields.map((f, index) => {
            return <DocumentRequiredAppVerType index={index} control={control} />
          }) : 
		  
		  
		  
		  {visibel ? fields.map((f, index) => {
            return <DocumentRequiredAppVerType index={index} control={control} />
          }) : ''}
